machine:
  timezone:
    America/Costa_Rica
    
dependencies:
  override:
    - sudo apt-get install -y awscli
  
test:
  override:
    - cucumber
    
deployment:
  production:
    branch: master
    commands:
      - aws s3 sync --delete . s3://cftemplates.nephelai.io
