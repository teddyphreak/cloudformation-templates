machine:
  timezone:
    America/Costa_Rica
  ruby: 
    version: 2.2.4
    
dependencies:
  override:
    - sudo apt-get install -y awscli
  
deployment:
  production:
    branch: master
    commands:
      - aws s3 sync --delete . s3://cftemplates.nephelai.io
